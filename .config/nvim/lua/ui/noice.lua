require("noice").setup({
    cmdline = {
        enabled = true,
        view = 'cmdline',
        format = {
            cmdline = { icon = ""}
        }
    },
    notify = {
        enabled = true,
        view = "notify"
    }
})
